

for (let i = 1 ; i<=10 ; i++)
{
    alert([`${'the number floor is'} ${[i]}`]);
}

// ---------------------------------------------

let mishmert

for ( let i = 1 ; i <= 10 ; i++)
{
    mishmert = prompt(("You are in or out "))
    if (mishmert === "in")
    {
        alert([`${'The hour now is'} ${[i]} ${'Yitzhak is watching !'}`]);  
    }
    else if (mishmert === "out")
    {
        alert([`${'The hour now is'} ${[i]} ${'Yitzhak is not !'}`]);  
    }

}
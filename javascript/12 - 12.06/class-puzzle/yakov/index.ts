export {};

// class puzzle:
import { students } from "../../../11 - 07.06/home-puzzle/yakov";
// 1. print in the console for each student in the list using Object.keys():
// key: value

let key = Object.keys(students);
console.log(students);
// keys.forEach(key => {
//   console.log(`${key}: `, person[key])
// })

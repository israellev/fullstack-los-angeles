//anonymous function

const getFullName = (firstName: string, lastName: string) => {
return `${firstName} ${lastName}`
}
const fullName = getFullName("Sahar","Klapisch")
console.log(`your full name is` + getFullName)
export {}

/*  short way to aggin variable by if else */
const index = 0
/*  long way */
let name;
if (index === 0) {
    name = "shir"
} else {
    name = "sapir"
}
/* short way */
const name2 = index === 0 ? "shir" : "sapir"


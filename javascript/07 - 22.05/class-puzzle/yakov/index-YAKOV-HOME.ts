export {};
// class puzzle:
// 1. Write a function that calls 'printState' that get 'str' parameter and print the fisrt and last string.
function printState(name: string) {
  // b=input.length[]
  console.log(name);
  console.log(name[0]);
  console.log(name[name.length - 1]);
}
printState("yakov");

 /* // 2. stringValidation
// Create a function that takes in a string, removes whitespace from both ends, converts it to lower case and then replaces all
 occurrences of the letter 'a' with 'b'. Make sure the function throws an error if something other than a string is input. 
// for example from " aaa " it will returs "bbb".
// hint: 'const isString = isNaN(Number(str))'
// (str: string) => string
 */




// home puzzle:

function stringManipulation(name: string) {}
